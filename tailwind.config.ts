// tailwind.config.ts
const config = {
  theme: {
    extend: {
      colors: {
        midnight: "#001A33",
        gold: "#D4AF37", // Un toque dorado para detalles premium
      },
      fontFamily: {
        serif: ['var(--font-playfair)', 'serif'],
        sans: ['var(--font-lato)', 'sans-serif'],
      }
    },
  },
}